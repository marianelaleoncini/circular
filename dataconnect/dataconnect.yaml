specVersion: "v1beta"
serviceId: "Circular"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "Circular-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
